import { Container, Typography } from '@material-ui/core';
import React from 'react';

const Contact = () => {
    return (
        <div>
            <Container>
                <Typography variant="h2">Contact Page is Under constaction</Typography>
            </Container>
        </div>
    );
};

export default Contact;